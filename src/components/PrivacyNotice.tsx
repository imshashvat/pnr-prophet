export default function PrivacyNotice() {
  return (
    <div className="text-xs text-muted-foreground mt-6">
      Privacy: We store your searches and eventual outcomes to improve predictions and calibration. No third-party keys are exposed to the browser. You can request deletion of your data from the Dashboard at any time. Basic rate limits protect service reliability.
    </div>
  );
}
